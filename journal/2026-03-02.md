- Refactored `welcome.py` hook to dynamically list commands from `.gemini/commands` and fixed "maintenance" typo across the project.
- Implemented `/cron` command with `cron.toml` task configuration and a background Python runner for scheduled execution via system crontab.
